/**
 * Placeholder database types. These are regenerated from the live schema with
 * `supabase gen types` (via the Supabase MCP `generate_typescript_types`) once
 * the schema is in place. Until then we fall back to a permissive type.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
